services:
  - type: web
    name: netflix-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python netflix_checker.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.9
